# Plan: M4-028a image build

- Status: Executed in `research/m4-028a-image-build`; the image built
  and verified on the dev host; all local gates green; awaiting human
  review, signed commit, and separately authorized publication.
- Date: 2026-09-06.
- Baseline: merged `891f0164ace7fc844b1488c905b9ddc6a6fd4404`.
- Authority: the accepted M4-028 entry recommendations (R1-R4,
  2026-09-06); [local issue](../../../planning/issues/028a-image-build.md).

## Tasks

1. Install the toolchain (edk2-ovmf, systemd-ukify 259.8 matched to
   the installed systemd, systemd-boot for the stub, sbsigntools) -
   done on the dev host; CI installs its own set in M4-028b.
2. `image/`: the key generator, the build script, the known command
   line, the README.
3. `scripts/test-image-build.py`: the fail-closed static verification.
4. Build + verify on the dev host (the host kernel 7.1.13 + its
   initramfs as payload, copied to a readable path); roadmap boundary,
   issue, plan.

## Executed evidence

- The build ran end to end: a 74.8MB signed UKI (sbverify: Signature
  verification OK against the committed test key) with
  .osrel/.cmdline/.uname/.linux/.initrd sections, .cmdline carrying
  exactly `ogir.test=1 console=ttyS0,115200 panic=-1`; the 100MB FAT
  ESP carrying EFI/BOOT/BOOTX64.EFI; the verification script PASS.
- Toolchain notes: the host initramfs is root-readable only (copy
  needed); the ESP must exceed the UKI size (64MB was too small for
  the 75MB UKI - the script uses 100MB).

## Deliberately not done

The QEMU/OVMF/swtpm boot harness and the fixture export (M4-028b);
the test-key-enrolled OVMF varstore (M4-028b); the signed reference
manifest (M4-029); the attack suite and exit audit (M4-030). The
build outputs (image/build/) are gitignored - they are products, not
sources.

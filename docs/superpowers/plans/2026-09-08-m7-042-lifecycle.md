# M7-042 plan: the session registry and the lifecycle cleanup matrix

Date: 2026-09-08
Agent: zcode
Authorization: standing publication+merge authorization; all gates
mandatory.

## Objective

Deliver the M7 lifecycle deliverable: the registry owning session
creation and the four-scenario cleanup matrix.

## Steps

1. Worktree research/m7-042-lifecycle from 8c78eb6.
2. The registry around the observation core: admit/state/cleanup/
   sweep/refresh + the after-loss semantic as an executable
   function.
3. The four-scenario matrix as the test set (plus duplicates,
   dead admissions, multi-session independence).
4. ADR-0038, index row, ROADMAP boundary, planning issue, plan.
5. House gates, signed commit, publication, CI, merge,
   post-merge verification.

## Development notes

- clippy's manual-Option::map prefers .map() over match-Some-
  None; a zombie-process panic path needs kill+wait (M7-041's
  lesson applied in the helper here).
- The settle helper from M7-041 is duplicated per test module by
  design (test-local, no shared test crate).

## Boundaries

In: registry + matrix + ADR-0038 + docs.
Out: event stream (M7-043); suite + audit (M7-044); persistence
(rejected).

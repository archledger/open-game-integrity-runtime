# M7-043 plan: the integrity event stream and renewal invalidation

Date: 2026-09-08
Agent: zcode
Authorization: standing publication+merge authorization; all gates
mandatory.

## Objective

Deliver the M7 stream deliverables: per-session bounded
integrity-change events and the sequence-based renewal gate.

## Steps

1. Worktree research/m7-043-event-stream from 2588161.
2. events.rs first (the log + gate as pure units), then the
   registry integration (refresh emits; exit terminalizes; gates).
3. The integration tests (quiet/exit/invalidation flow).
4. ADR-0039, index row, ROADMAP boundary, planning issue, plan.
5. House gates, signed commit, publication, CI, merge,
   post-merge verification.

## Development notes

- fmt reflows long signatures between edits: always re-grep the
  CURRENT text before a scripted replacement (two assertions
  failed on stale anchors this slice).
- Nested if-let chains collapse with let-chains (the 1.98
  toolchain supports them in edition 2024).
- clippy rejects assert_eq! on literal bools.

## Boundaries

In: events module + registry integration + ADR-0039 + docs.
Out: suite + audit (M7-044); persistence (rejected); M6 route
wiring (later composition).

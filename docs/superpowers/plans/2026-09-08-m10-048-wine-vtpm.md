# M10-048 plan: the per-prefix virtual TPM

Date: 2026-09-08
Agent: zcode
Authorization: standing authorization extended through M10.

## Objective

Open M10: the per-prefix vTPM manager plus its mechanical gate
and the capability contract.

## Steps

1. Worktree research/m10-048-wine-vtpm from ae2492f.
2. The manager (LGPL) with the structural isolation design; the
   smoke test first.
3. The four-family gate (LGPL).
4. The capability contract in wine/README.md; ADR-0044; ROADMAP
   boundary; issue; plan.
5. Gates, commit, publish, CI, merge, verify; dispositions.

## Development notes

- wine/ source must be LGPL-2.1-or-later (the metadata gate
  enforces it - a first for this repo's Wine track).
- The socket key is a HASH of the prefix path (not the path
  itself: no user-path material in the runtime dir).

## Boundaries

In: manager + gate + contract + ADR-0044.
Out: TBS implementation; the remaining families; the audit.
